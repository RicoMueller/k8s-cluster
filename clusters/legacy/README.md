# Legacy Cluster Manifests (Pre-Pantheon)

This directory contains manifests from the previous cluster setup, kept for
historical reference. These manifests are **not reconciled** by Flux CD —
they exist only in Git history.

Migration to the new Pantheon-Cluster happens app-by-app.
