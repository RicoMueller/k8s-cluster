apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: qnap-trident
  namespace: trident
spec:
  interval: 1h
  chart:
    spec:
      chart: ./infrastructure/controllers/storage/qnap-iscsi/chart
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
