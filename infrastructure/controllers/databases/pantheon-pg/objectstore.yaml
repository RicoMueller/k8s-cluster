apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: pantheon-pg-store
  namespace: databases
spec:
  configuration:
    destinationPath: s3://pantheon-cnpg-backup/
    endpointURL: https://fsn1.your-objectstorage.com
    s3Credentials:
      accessKeyId:
        name: pantheon-pg-backup-s3
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: pantheon-pg-backup-s3
        key: ACCESS_SECRET_KEY
    wal:
      compression: gzip
    data:
      compression: gzip
