apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: pantheon-pg-backup
  namespace: databases
spec:
  schedule: "0 0 2 * * *"
  backupOwnerReference: self
  cluster:
    name: pantheon-pg
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
